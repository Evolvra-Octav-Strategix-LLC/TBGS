# TBGS Email Service PHP - Environment Configuration Example
# Copy this file to .env and configure your settings

# SMTP Configuration (Gmail recommended)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Email Settings
CONTACT_RECEIVER=info@tbgs.nl

# CORS Settings (comma-separated origins, or * for all)
CORS_ORIGINS=https://tbgs.nl,http://localhost:3000,*

# Application Environment
APP_ENV=production

# Optional: Custom logging and debugging
LOG_LEVEL=info

# Note: For Gmail, you need to:
# 1. Enable 2-factor authentication
# 2. Generate an "App Password" for SMTP_PASS
# 3. Use that App Password instead of your regular password